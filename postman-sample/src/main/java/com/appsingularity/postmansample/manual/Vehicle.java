package com.appsingularity.postmansample.manual;

// This base class was *not* coded with parceling in mind, so it's probably not safe to process it
public class Vehicle {
    String mType;

    public Vehicle() { }

}
